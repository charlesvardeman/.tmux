session_name: "${PROJECT}"
start_directory: "${DIR}"
windows:
  - window_name: code
    layout: even-vertical
    focus: true
    panes:
      - shell_command:
        focus: true
  - window_name: misc
    layout: tiled
    panes:
      - pane
      - pane
      - pane
      - pane
